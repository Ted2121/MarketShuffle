import { Box } from '@mui/material';
import React, { useState } from 'react'

export default function SWHeroes({type, icon, text}) {
    return (
        <Box sx={{
            // width: '100%',
            // height: '100%',
            display: 'flex',
            // justifyContent: 'center',
            // alignItems: 'center',
            // flexDirection: 'column',
            gap: 2,
            marginTop: 10,
            marginX: 10,
        }}>
            
        </Box>
    );
}